# Agent Instructions

本仓库维护 `Business-Unit-for-Energy-Saving` 的 GitHub profile 和公开导航。

## 规则

- 修改前先读取 `README.md`、`profile/README.md` 和相关 `docs/`。
- 本仓库只放组织定位、公开入口、仓库地图和轻量治理说明。
- 具体业务代码、需求、数据资产和部署配置放在对应业务仓库。
- 不提交 token、密码、SSH key、服务器凭据、客户隐私或未授权数据。
- 简单 README/docs 更新可以直接维护 `main`。
- 代码、部署、CI、权限和架构变更应使用分支和 PR。

## 验证

```bash
git status --short
git diff --check
```
